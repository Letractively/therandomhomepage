<?xml version="1.0" encoding="ISO-8859-1"?>
<project name="TheRandomHomepage-Java" default="" basedir=".">

    <property file="${user.home}/user.properties"/>
    <property name="build.dir" value="./build"/>
    <property name="backup.dir" value="${build.dir}/backups"/>
    <property name="lib.dir" value="../../lib"/>

    <tstamp>
        <format property="build.time" pattern="MMMM-dd-yyyy-HH-mm"/>
    </tstamp>    

    <target name="deployRandomFlickrGadget" description="FTP">
        <ftp server="${ftp_host}"
             remotedir="dev/gadgets/RandomFlickr"
             userid="${user_id}"
             password="${password}"
             depends="yes"
             verbose="yes"
                >
            <fileset dir="out/therandomhomepage.RandomFlickr"/>
        </ftp>
    </target>

    <target name="deployLightbox" description="FTP">
        <ftp server="${ftp_host}"
             remotedir="dev/google/gadgets/RandomFlickr"
             userid="${user_id}"
             password="${password}"
             depends="yes"
             verbose="yes"
                >
            <fileset dir="C:\downloads\lightbox2.02"/>
        </ftp>
    </target>

    <target name="init">
        <mkdir dir="${build.dir}"/>
        <mkdir dir="${backup.dir}"/>
    </target>

    <target name="backupWebsite" depends="init">
        <echo>Backing up ${ftp_host}...</echo>
        <!--<ftp action="get"-->
        <!--server="${ftp_host}"-->
        <!--userid="${user_id}"-->
        <!--password="${password}">-->
        <!--<fileset dir="${backup.dir}">-->
        <!--<include name="**/*"/>-->
        <!--<include name="forum/**"/>-->
        <!--<exclude name="**/cache/**"/>-->
        <!--<exclude name="**/_vti_cnf/**"/>-->
        <!--</fileset>-->
        <!--</ftp>-->

        <!--<jar destfile="${build.dir}/${build.time}.jar"-->
        <!--basedir="${backup.dir}"-->
        <!--includes="**"-->
        <!--/>-->

        <!--<mail mailhost="smtp.gmail.com" from="me" tolist="${backup_mailbox}" subject="Backup files for ${ftp_host} at {build.time}"-->
        <!--files="${build.time}.jar"/>-->

        <mail mailhost="siddii@gmail.com" mailport="25" subject="Test build" charset="utf-8">
            <from address="siddii@gmail.com"/>
            <to address="siddii@gmail.com"/>
            <message>some text</message>
        </mail>
    </target>

</project>